</html> <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Product List</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background-color: #f8f9fa;
            padding-top: 50px;
        }

        .container {
            max-width: 900px;
            background: #fff;
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        }

        h1 {
            text-align: center;
            margin-bottom: 30px;
            font-weight: bold;
        }

        .table th,
        .table td {
            vertical-align: middle;
        }

        .btn-add {
            margin-bottom: 20px;
        }

        .no-data {
            text-align: center;
            font-style: italic;
        }
    </style>
</head>
<body>

    <a href="/" class="btn btn-outline-primary top-left-button">← Back to Home</a>
<div class="container">
    <h1>Product List</h1>

    <div class="text-end">
        <a href="/products/add" class="btn btn-primary btn-add">Add New Product</a>
    </div>

    <table class="table table-bordered table-hover">
        <thead class="table-dark">
            <tr>
                <th>Product ID</th>
                <th>Product Name</th>
                <th>Category ID</th>
                <th>Category Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        <% if (products.length === 0) { %>
            <tr>
                <td colspan="5" class="no-data">No products found</td>
            </tr>
        <% } else { %>
            <% products.forEach(product => { %>
                <tr>
                    <td><%= product.pid %></td>
                    <td><%= product.product_name %></td>
                    <td><%= product.categoryId %></td>
                    <td><%= product.categoryName %></td>
                    <td>
                        <a href="/products/edit/<%= product.pid %>" class="btn btn-sm btn-warning">Edit</a>
                        <a href="/products/delete/<%= product.pid %>" class="btn btn-sm btn-danger" onclick="return confirm('Are you sure you want to delete this product?')">Delete</a>
                    </td>
                </tr>
            <% }) %>
        <% } %>
        </tbody>
    </table>
</div>

</body>
</html>